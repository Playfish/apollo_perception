#dodge the obstacle in lateral direction when driving

ObjectNudgeType type

# minimum lateral distance in meters. positive if type = LEFT_NUDGE
# negative if type = RIGHT_NUDGE
float64 distance_l  #  optional
